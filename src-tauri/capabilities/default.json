{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window, the break overlay window, and the startup catch-up window",
  "windows": [
    "main",
    "overlay",
    "catchup"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "opener:default",
    "sql:default",
    "sql:allow-execute",
    "updater:default",
    "process:default"
  ]
}